# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
node_modules/

# Scratch queries saved by the local Supabase Studio SQL editor — per-developer, not project files
supabase/snippets/
